version: '3'

services:
  web:
    entrypoint: /entrypoint.sh
    build: .
    command: python Source/manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    depends_on:
      - db
  db:
    image: postgres:latest